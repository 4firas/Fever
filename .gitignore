# Swift / SwiftPM
.build/
.swiftpm/
DerivedData/
*.xcodeproj

# macOS
.DS_Store

# Built app bundle
*.app
dist/

# Bundled runtime + model assets (fetched on setup, not committed)
Sidecar/.venv/
Sidecar/python/
Models/*.task
Models/*.tflite
*.whl

# Local capture footage / pasted scratch images
*.mp4
*.mov
Pasted*.png

# Local scratch + working notes
FEVER-SPEC.json
NIGHT-REFINEMENT-LOG.md
pi-session-recap.md
.rotation-fix.js
.vrchat-fixes.js
docs/
Package.resources/
*.onnx
.coreml_cache/
**/.venv/
*.coreml

# Non-distributable reverse-engineered model IP + heavy local media (never commit)
fbt_re/
fever-media/
# Gated SMPL-X body models (license-restricted; user-provided locally, never redistribute)
models/
*.pkl
*.npz
*.pyd
*.engine
*.enc

# PC offload daemon (PinoFBT-derived, non-distributable)
pc-daemon/

# On-device NLF runtime (venv + non-distributable model)
nlf-runtime/
LOOP_LOG.md
